---
import client from "../../tina/__generated__/client";
const globalResp = await client.queries.global({ relativePath: "index.md" });
const profilePicture = globalResp.data.global.profilePicture;
---

<img src={profilePicture} alt="Profile picture" class="twitter-avatar" />
